package myproject;
import java.lang.Math.*;

public class Matheg {
	public static void main(String[] args)
	{
		
		double power = Math.pow(2,2);
		System.out.println("power = " + power);
		double num=4;
		System.out.println("sqare root = " + Math.sqrt(num));
		double area=Math.PI*num*num;
		System.out.println("area = " + area);
		System.out.println("roundnum = " + Math.ceil(area));
		
		
	}

}
